angular.module('lite')
    .component('footerComponent', {

        templateUrl: 'app/components/app-footer/footer-component.html',
        bindings: {
            //here we put bindings if we need some
        }
    });