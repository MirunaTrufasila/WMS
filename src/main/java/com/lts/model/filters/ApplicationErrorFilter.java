package com.lts.model.filters;

public enum ApplicationErrorFilter {

    DATA_MIN,
    DATA_MAX,
    USERNAME,
    FIXED,
    EMAIL_SENT,
    LEVEL,
    MESSAGE,
    ORIGIN,
    APPLICATION,
    ID
}
