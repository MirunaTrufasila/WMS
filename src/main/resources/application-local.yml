spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lite_local?zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: false

server:
  port: 8082
  servlet:
    context-path: /lts